# musicapp

步骤
1. vue init webpack musicApp
2. 文件目录设置
3. 样式文件 stylus
4. 安装需要的loader等
5. webpack别名配置
6. jsonp库的使用,在js中的jsonp.js中封装，使用promise来封装
7. 封装请求数据的方法，在src/api文件夹下创建js文件。把jsonp请求通用的封装在src/api/config.js